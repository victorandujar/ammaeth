export interface EssenceColors {
  id: string;
  text: string;
  color: string;
}

export interface EssenceSimbol {
  id: string;
  title: string;
  text: string;
  subtext: string;
  image: string;
}
